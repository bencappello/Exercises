<%= render partial: "form", locals: {
  track: @track,
  album_id: @album_id,
  title: "Edit #{@track.name}",
  action_url: track_url(@track),
  form_method: "PATCH",
  submit_text: "Update track"
  } %>
sdfh
